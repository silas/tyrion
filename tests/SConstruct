source = [
    '../src/config.cc',
    '../src/process.cc',
    '../src/third_party/inih/ini.c',
    '../src/utils.cc',
    'test.cc',
]

library_list = ['gtest', 'pthread', 'txmpp']

env = Environment()
env.Program(target='test-all', source=source, LIBS=library_list)

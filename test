#!/usr/bin/env bash

run-tests() {
  pushd test-src
  scons && \
  ./test-config && \
  ./test-envelope && \
  ./test-logging && \
  ./test-process && \
  ./test-settings
  retval=$?
  popd
  return $retval
}

run-tests
